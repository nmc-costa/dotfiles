---
description: Cross-session task brief — what's pending and what needs the owner (tasks/brief.py).
---

Use the `task-brief` skill (loaded from `~/.agents/skills/task-brief/SKILL.md`).

Arguments (optional): $ARGUMENTS

Run `python3 tasks/brief.py` from the canonical checkout (`~/dotfiles`), then
report what needs the owner's attention in keyword-compact form.
