# CLAUDE.md

Personal project context — loaded automatically when working in `~/Projects/`.

## Context

This is **personal project territory** — initiatives, studies, experiments, and hobby code. No company rules, no proprietary content, no client PII.

Convention: each repo is named descriptively (e.g., `roi_lab`, `agentic_instructions`, `notes`), with its own `CLAUDE.md` and `README.md` if it's a substantial project.

See parent `~/CLAUDE.md` and `@~/dotfiles/AGENTS.md` for full workspace guidance.

---

*Note: Claude Code automatically concatenates CLAUDE.md files from multiple directory levels, so this file inherits everything from `~/CLAUDE.md`. Only add here if it's specifically personal-context guidance.*
