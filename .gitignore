# Local environment
.DS_Store
.swp
.swo
*~
*.swp
*.bak

# IDE
.vscode/*
# settings.json contains a real API key and is managed by chezmoi+age
# (see SECRETS.md) — it must never be committed as plaintext again.
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/

# dtx-providers: secrets.env (chezmoi+age, see docs/SECRETS.md) plus everything
# else here is generated locally (litellm-config.yaml embeds a real upstream API
# key server-side, proxy.env is a locally-generated token) — never commit any of it.
.dtx-providers/*
*.sublime-*

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Logs and runtime
*.log
.env
.env.local
*.pid

# OS
Thumbs.db
node_modules/

# Temporary
temp/
tmp/
*.tmp
.cache/

# Agent-specific (don't track user sessions/logs)
.claude/sessions/
.claude/logs/
.claude/cache/
.agents/cache/
.copilot/sessions/
.cline/sessions/

# But DO track core configs
!.claude/
!.agents/
!.vscode/
!.github/
